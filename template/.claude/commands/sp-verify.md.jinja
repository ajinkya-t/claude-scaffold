---
description: Verification before completion — evidence over claims (Sonnet). Wraps {{ sp_prefix }}:verification-before-completion.
model: {{ model_mid }}
---

Invoke the `{{ sp_prefix }}:verification-before-completion` skill and follow it exactly. Sonnet tier — runs verification commands and confirms output before any "done" claim.
