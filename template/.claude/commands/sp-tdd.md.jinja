---
description: RED-GREEN-REFACTOR cycle (Sonnet). Wraps {{ sp_prefix }}:test-driven-development.
argument-hint: <feature or behavior to implement>
model: {{ model_mid }}
---

Behavior: $ARGUMENTS

Invoke the `{{ sp_prefix }}:test-driven-development` skill and follow it exactly. Sonnet tier — TDD discipline is well-defined; the skill enforces the cycle, the model executes it.
