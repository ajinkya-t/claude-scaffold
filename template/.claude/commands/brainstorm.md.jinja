---
description: Brainstorm a design (Opus). Wraps {{ sp_prefix }}:brainstorming with the high tier pinned.
argument-hint: <topic>
model: {{ model_high }}
---

Topic: $ARGUMENTS

Invoke the `{{ sp_prefix }}:brainstorming` skill and follow it exactly. This shim pins the Opus tier because brainstorming benefits from depth — clarifying questions, alternative exploration, and design framing reward reasoning over speed.
