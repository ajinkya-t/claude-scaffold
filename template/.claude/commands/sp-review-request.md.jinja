---
description: Pre-review checklist before requesting code review (Sonnet). Wraps {{ sp_prefix }}:requesting-code-review.
model: {{ model_mid }}
---

Invoke the `{{ sp_prefix }}:requesting-code-review` skill and follow it exactly. Sonnet tier — the skill is a structured pre-flight; the model walks the checklist.
